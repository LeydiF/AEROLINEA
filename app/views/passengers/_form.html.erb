<%= form_for(@passenger) do |f| %>
  <% if @passenger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@passenger.errors.count, "error") %> prohibited this passenger from being saved:</h2>

      <ul>
      <% @passenger.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :apellido %><br>
    <%= f.text_field :apellido %>
  </div>
  <div class="field">
    <%= f.label :identificacion %><br>
    <%= f.number_field :identificacion %>
  </div>

  <%= f.select :vuelo, Route.all.map{|r| [r.vuelo, r.vuelo]}%>
<!--   <div class="field">
    <%= f.label :vuelo %><br>
    <%= f.text_field :vuelo %>
  </div> -->
  <div class="field">
  <p>Seleccione un vuelo: </p>
<!--   <% Route.all.each do |vuelo1|%> 

    <%= check_box_tag "vuelo[]", vuelo1.id %>
    <%= vuelo1.vuelo %><br> 
  <% end %> -->
</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
